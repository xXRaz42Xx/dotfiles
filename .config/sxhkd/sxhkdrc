#!/bin/sh

# ════════════════════════════════════════════════════════════
# 🚀 APPLICATIONS
# ════════════════════════════════════════════════════════════

# Terminal emulator
super + t
    kitty

# Application launcher (Rofi)
super + a
    rofi -show drun -theme ~/.config/rofi/launchers/type-1/style-1.rasi

# Window switcher
super + Tab
    rofi -show window -theme ~/.config/rofi/launchers/type-1/style-1.rasi

# Run prompt
super + shift + p
    rofi -show run -theme ~/.config/rofi/launchers/type-1/style-1.rasi

# Web browser
super + b
    firefox

# ════════════════════════════════════════════════════════════
# 🔧 BSPWM HOTKEYS
# ════════════════════════════════════════════════════════════

# Quit/restart bspwm
super + alt + {q,r}
    bspc {quit,wm -r}

# Close and kill window
super + {_,shift + }q
    bspc node -{c,k}

# Reload sxhkd
super + Escape
    pkill -USR1 -x sxhkd && notify-send "SXHKD" "Configuration reloaded"

# ════════════════════════════════════════════════════════════
# 🪟 WINDOW STATE AND FLAGS
# ════════════════════════════════════════════════════════════

# Toggle floating/tiled
super + w
    if [ -z "$(bspc query -N -n 'focused.floating')" ]; then \
        bspc node -t floating; \
    else \
        bspc node -t tiled; \
    fi

# Set other states
super + {t,shift + t,f}
    bspc node -t {tiled,pseudo_tiled,fullscreen}

# Toggle monocle layout
super + m
    bspc desktop -l next

# ════════════════════════════════════════════════════════════
# 🎯 FOCUS/SWAP WINDOWS
# ════════════════════════════════════════════════════════════

# Focus/swap window in given direction
super + {_,shift + }{h,j,k,l}
    bspc node -{f,s} {west,south,north,east}

# Focus/swap window (arrow keys)
super + {_,shift + }{Left,Down,Up,Right}
    bspc node -{f,s} {west,south,north,east}

# Focus the node for the given path jump
super + {p,b,comma,period}
    bspc node -f @{parent,brother,first,second}

# Focus next/previous window in current desktop
super + {_,shift + }c
    bspc node -f {next,prev}.local.!hidden.window

# Focus the last node/desktop
super + {grave,grave}
    bspc {node,desktop} -f last

# Focus the older or newer node in focus history
super + {o,i}
    bspc wm -h off; \
    bspc node {older,newer} -f; \
    bspc wm -h on


# ════════════════════════════════════════════════════════════
# 🗂️  DESKTOP MANAGEMENT
# ════════════════════════════════════════════════════════════

# Focus or send to desktop
super + {_,shift + }{1-9,0}
    bspc {desktop -f,node -d} '^{1-9,10}'

# Focus next/previous desktop in current monitor
super + bracket{left,right}
    bspc desktop -f {prev,next}.local

# Focus the next/previous desktop
super + ctrl + {Left,Right}
    bspc desktop -f {prev,next}.local

# Send window to next/previous desktop and follow
super + shift + ctrl + {Left,Right}
    bspc node -d {prev,next} --follow

# ════════════════════════════════════════════════════════════
# 🖥️  MONITOR FOCUS
# ════════════════════════════════════════════════════════════

# Focus the next/previous monitor
super + ctrl + {bracketleft,bracketright}
    bspc monitor -f {prev,next}

# Send the focused window to the next/previous monitor
super + ctrl + shift + {bracketleft,bracketright}
    bspc node -m {prev,next} --follow

# ════════════════════════════════════════════════════════════
# 📐 PRESELECTION
# ════════════════════════════════════════════════════════════

# Preselect direction
super + ctrl + {h,j,k,l}
    bspc node -p {west,south,north,east}

# Preselect direction (arrow keys)
super + ctrl + {Left,Down,Up,Right}
    bspc node -p {west,south,north,east}

# Preselect ratio
super + ctrl + {1-9}
    bspc node -o 0.{1-9}

# Cancel preselection for focused node
super + ctrl + space
    bspc node -p cancel

# Cancel preselection for focused desktop
super + ctrl + shift + space
    bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

# ════════════════════════════════════════════════════════════
# 🔄 MOVE/RESIZE WINDOWS
# ════════════════════════════════════════════════════════════

# Expand window by moving sides outward
super + alt + {h,j,k,l}
    bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# Contract window by moving sides inward
super + alt + shift + {h,j,k,l}
    bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# Move floating window
super + {Left,Down,Up,Right}
    bspc node -v {-20 0,0 20,0 -20,20 0}

# ════════════════════════════════════════════════════════════
# 🎨 ROFI MENUS (HyDE Style)
# ════════════════════════════════════════════════════════════

# Keybindings menu
super + slash
    ~/.config/rofi/scripts/keybindings.sh

# ════════════════════════════════════════════════════════════
# 🎨 SYSTEM CONTROLS
# ════════════════════════════════════════════════════════════

# Lock screen
super + x
    betterlockscreen -l dim
    # Or: i3lock-fancy / slock

# Reload polybar
super + shift + b
    ~/.config/polybar/launch.sh

# ════════════════════════════════════════════════════════════
# 🔊 QUICK CONTROLS
# ════════════════════════════════════════════════════════════

# Show volume notification
super + shift + v
    ~/.config/dunst/scripts/show-volume.sh

# Show brightness notification  
super + shift + n
    ~/.config/dunst/scripts/show-brightness.sh

# System monitor
super + shift + m
    kitty --class=floating -e btop

# ════════════════════════════════════════════════════════════
